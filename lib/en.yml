en:
  remarkable:
    thinking_sphinx: 
      index: 
        description: "should index {{field}}"
        expectations: 
          indexes: "{{subject_name}} should index {{field}}"
        optionals:
          as:
            positive: "as {{inspect}}"
      have_index_attribute:
        description: "index should include {{attribute}} as an attribute"
        expectations: 
          has_index_attribute: "{{subject_name}} index should have {{attribute}}"
