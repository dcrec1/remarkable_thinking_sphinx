en:
  remarkable:
    thinking_sphinx: 
      index: 
        description: "should index {{attribute}}"
        expectations: 
          indexes: "{{subject_name}} should index {{attribute}}"
        optionals:
          as:
            positive: "as {{inspect}}"
      have_index_attribute
        description: "index should include {{attribute}} as an attribute"
        expectations: 
          has_index_attribute: "{{subject_name}} index should have {{attribute}}"
