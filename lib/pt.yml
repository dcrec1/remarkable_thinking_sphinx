pt:
  remarkable:
    thinking_sphinx: 
      index: 
        description: "indexar {{field}}"
        expectations: 
          indexes: "{{subject_name}} indexe {{field}}"
        optionals:
          as:
            positive: "como {{inspect}}"
      have_index_attribute:
        description: "incluir {{attribute}} como atributo no index"
        expectations: 
          has_index_attribute: "{{subject_name}} inclua {{attribute}} como atributo no índice"
