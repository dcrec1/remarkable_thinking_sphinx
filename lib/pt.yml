pt:
  remarkable:
    thinking_sphinx: 
      index: 
        description: "deve indexar {{field}}"
        expectations: 
          indexes: "{{subject_name}} deve indexar {{field}}"
        optionals:
          as:
            positive: "como {{inspect}}"
      have_index_attribute:
        description: "index deve incluir {{attribute}} como atributo"
        expectations: 
          has_index_attribute: "o index de {{subject_name}} deve ter um {{attribute}}"
